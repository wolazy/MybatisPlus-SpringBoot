<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.haolan.dao.base.BaseUserMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		 userId, userName, loginName, userRemark, userEmail, userPhoto, password, userMobile, createDate, updateDate
	</sql>
	<select id="queryAll" resultType="BaseUser">
		select * from b_base_user
	</select>
	<select id="selectPageUser" resultType="BaseUser">
		 select * from b_base_user
	</select>
</mapper>